using UnityEngine;

public class Calliope : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("This is a lucky mystery box");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
